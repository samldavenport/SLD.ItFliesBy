#pragma once

#include <Windows.h>

#include "sld.hpp"

int WINAPI
wWinMain(
    HINSTANCE h_instance,
    HINSTANCE h_prevInstance,
    PWSTR     p_cmd_line,
    int       n_cmd_show) {

    return(0);
}